{
  "title": "acp-node-v2: deliverable content tagged system-role reaches the evaluator's LLM unfiltered (original finding, Aug 2026)",
  "goal": "escrow_released_on_injected_verdict",
  "nodes": {
    "provider_controls_deliverable": "`JobSession.submit(deliverable: string, ...)` takes an arbitrary, unvalidated string from the provider — no schema, no length limit, no content check (verified directly, src/jobSession.ts:606-626; AcpJob.deliverable typed `string | null`, src/acpJob.ts:72,80,95)",
    "deliverable_tagged_system_role": "Root cause, core SDK: `JobSession.toMessages()` builds `The provider has submitted a deliverable: ${this._job?.deliverable}` and pushes it typed `role: \"system\"` — the deliverable string is interpolated verbatim, with no sanitisation or delimiter fencing (verified directly, src/jobSession.ts:720-736, specifically the content-build at 721-722 and the push at 736)",
    "example_coerces_system_to_user": "Channel collapse, shipped reference code: since Anthropic's Messages API has no inline system-role message, both official LLM examples map `role: \"system\"` to `role: \"user\"` and string-concatenate it onto the adjacent turn — untrusted deliverable text and real conversation content end up in one undifferentiated user turn (verified directly, src/examples/llm/seller.ts and src/examples/llm/buyer.ts, function `toAnthropicMessages()`: `const role = m.role === \"system\" ? \"user\" : m.role`)",
    "evaluator_context_poisoned": "The poisoned note reaches the evaluator-role agent's LLM call as ordinary conversational context, with no warning that its content originates from an untrusted counterparty (direct consequence of the two nodes above; no compensating mitigation exists anywhere in this path — contrast with the explicit in-prompt warning present in Candidate 5 of cases/erc8183-evaluator-integrity)",
    "evaluator_role_grants_complete_on_submitted": "Structural gate, not a mitigation: the SDK's own tool-availability matrix grants `complete`/`reject` to the `evaluator` role specifically when job status is `submitted` — i.e. exactly when the poisoned deliverable note is freshest in context — and `job.submitted` is routed only to agents holding the evaluator role (verified directly, src/jobSession.ts TOOL_MATRIX.evaluator.submitted and RESPONDERS[\"job.submitted\"] = [\"evaluator\"])",
    "verdict_flipped": "The LLM complies with an embedded directive in the deliverable and calls the `complete` tool; `executeTool()` only checks role/status availability, not content — no human confirmation, no re-check (verified directly, src/jobSession.ts executeTool() dispatch to complete(), and src/acpAgent.ts:1004 internalComplete())",
    "escrow_released_on_injected_verdict": "On-chain `complete()` executes on the manipulated verdict, releasing escrow. In the SDK's own shipped buyer.ts reference example, the buyer sets itself as the job's evaluator (`evaluatorAddress: buyerAddress`, src/examples/llm/buyer.ts:349) — client-as-evaluator is not a hypothetical edge case here, it's the example's default. Independently corroborated on-chain: virtuals-forensics (same author, same protocol family) found 212 real addresses already operating as client==evaluator, and 98.5% of empty/near-empty deliverables still resulted in payment release."
  },
  "edges": [
    { "id": "e1", "tail": ["provider_controls_deliverable", "deliverable_tagged_system_role", "example_coerces_system_to_user"], "head": "evaluator_context_poisoned" },
    { "id": "e2", "tail": ["evaluator_context_poisoned", "evaluator_role_grants_complete_on_submitted"], "head": "verdict_flipped" },
    { "id": "e3", "tail": ["verdict_flipped"], "head": "escrow_released_on_injected_verdict" }
  ],
  "sigma": [],
  "sigma_completeness": "best_effort",
  "_note_for_reader": "Обнаружено и верифицировано 2026-08-05 путём прямого чтения кода marsakahenry14-lab/acp-node-v2 (форк Virtual-Protocol/acp-node-v2, синхронизирован непосредственно перед анализом, commit 8af150b, 2026-07-28) — независимо от disclosure, отправленного автором тому же вендору 2026-06-29 (см. SOURCES.md, раздел Disclosure). Сама находка совпадает: тот же файл, та же функция, тот же механизм — по всей видимости, переоткрыта, а не позаимствована. Disclosure-окно (30 дней с 29 июня) закрылось 29 июля; ответа от Virtuals не последовало ни разу.\n\nЭто линейная цепочка (все 6 узлов + цель — LB при sigma=[]), намеренно НЕ форсирована в дизъюнкцию: существует второй, структурно похожий injection surface (job.description/offeringName, попадающий в role:\"system\" через seller.ts's offeringContextNote — verified directly, но это EXAMPLE-код, не core SDK), но он манипулирует ДРУГИМ актором (seller/provider, не evaluator) в направлении ДРУГОГО вреда (ценовое поведение продавца, не освобождение эскроу) — объединение его с текущей целью было бы натяжкой. Описан отдельно в SOURCES.md.\n\nВАЖНОЕ ОТЛИЧИЕ от cases/potpie-graphrag-prompt-injection (тоже линейная цепочка): та цепочка историческая и подтверждённо не существует в текущем коде. Эта — ЖИВАЯ, в текущем форке, прямо сейчас, без единой компенсирующей меры (ни delimiter fencing, ни explicit warning модели, ни structural gate — ничего из того, что смягчало часть сценариев в cases/erc8183-evaluator-integrity)."
}
